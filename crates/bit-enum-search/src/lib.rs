use bitset_fixed::BitSet;

pub struct BitEnum {
    cur: usize,
    n: usize,
}

impl BitEnum {
    fn new(n: usize) -> Self {
        Self { cur: 0, n }
    }

    fn gen(&mut self) -> BitSet {
        BitSet::
    }
}
